pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'kotlin-commerce-msa'

include(
    'api-gateway',
    'product-service',
    'inventory-service',
    'payment-service',
    'order-service'
)

