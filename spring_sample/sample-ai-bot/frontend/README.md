# frontend

React + Vite 기반 채팅 UI다.

- 채팅 통신은 `VITE_WS_URL`(기본 `ws://localhost:8088/ws/chat`) WebSocket을 사용한다.

## 실행

```bash
cd /Users/revy/workspace_codex/spring_sample/sample-ai-bot/frontend
npm install
npm run dev
```

## 빌드

```bash
npm run build
```
