server:
  port: 8080

spring:
  application:
    name: bllue-green-docker-app

deploy:
  color: ${DEPLOY_COLOR:unknown}
  version: ${DEPLOY_VERSION:local}

management:
  endpoints:
    web:
      exposure:
        include: health,info
