# 작업 계획

1. `blue-green-nginx` 디렉터리에 독립 실행형 Spring Boot 앱 프로젝트를 생성한다.
2. Docker 없이 로컬 프로세스 + Nginx reload 기반 Blue-Green 배포/롤백/상태 스크립트를 `sh`로 작성한다.
3. 배포 슬롯(`blue`, `green`)별 PID, 로그, 버전, 헬스체크를 관리하는 런타임 구조를 구성한다.
4. 루트 `README.md`, `TASK.md`에 신규 프로젝트 실행/검증/작업 내역을 반영한다.
