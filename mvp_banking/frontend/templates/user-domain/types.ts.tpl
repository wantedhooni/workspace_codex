export type __DOMAIN_PASCAL__Item = {
  id: string;
  code: string;
  name: string;
  status: string;
  createdAt: string;
};

export type Create__DOMAIN_PASCAL__Payload = {
  code: string;
  name: string;
  note?: string;
};
